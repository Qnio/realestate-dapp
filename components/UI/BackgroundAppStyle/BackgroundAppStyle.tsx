import {BackgroundImage, HeroContainer} from "./BackgroundAppStyle.styles";
import React from "react";

const BackgroundAppStyleStyles: React.FC = () => {
    return (
        <HeroContainer>
            <BackgroundImage/>
        </HeroContainer>
    )
}

export default BackgroundAppStyleStyles;
